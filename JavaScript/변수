변수(variable): 숫자 또는 문자열과 같은 값의 컨테이너, 포함된 값이 변경될 수 있다. 숫자, 문자열 뿐만 아니라 
                데이터 및 함수도 포함할 수 있으며 변수는 값 자체가 아니라 값을 위한 (물건을 저장할 수 있는 상자같은) 컨테이너 이다.

변수의 선언: Var 변수이름;

변수 이름을 입력하여 변수의 값이 실행 환경에 존재하는지 테스트 할 수있다: 변수이름; ex) var myName;
  존재하지만 값을 포함하고 있지 않은 변수: undefined를 반환.
  존재하지 않는 변수: Error

변수의 초기화: 변수 선언후 등호(=)와 그뒤에 값을 입력하여 초기화 할 수있다. ex) myName = 'Joe';
  변수를 선언하고 동시에 초기화 할 수있다. ex) var myName = 'Joe';

변수 이름에 대한 규칙:
  1.변수 시작부분에 밑줄(_) 혹은 숫자를 사용하지 말것.
  2.문장을 쓸경우 여러 단어를 하나로 묶고 첫 단어의 시작은 소문자, 그다음은 대문자 사용 ex) myAge, myName
  3.변수는 대소문자를 구분 ex myage와 myAge는 전혀 다른 변수.
  4.JavaScript 예약어(var, let, function, for등)를 변수로 사용하면 안됨.

변수의 유형:
  숫자(number): 정수 혹은 소수점 숫자를 변수에 저장할수 있으며 이때 따옴표는 사용하지 않는다.
  문자열(string): 문자열 값을 변수에 대입할 때 작은따옴표(')나 큰따옴표(")로 묶는다.
  불리언(Booleans): true혹은 false라는 값을 가지는 참/거짓을 표현하는 데이터 유형.
  배열(Array): 대괄호로 묶이고 쉼표로 구분된 여러 값을 포함하는 단일 객체.
    var myNumberArray = [5,10,15,20];
    myNumberArray[0]; -> 5가 반환
    myNumberArray[3]; -> 25이 반환
    대괄호에는 반환할 값의 위치를 정하는 index 값이 들어있는데 컴퓨터는 1이 아니라 0부터 센다.
 
 객체: 실제 사물을 모델링 하는 코드구조 ex) var dog = {name: 'Spot', breed: 'Dalmatian'};
